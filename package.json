{
  "name": "ai-career-guidance",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "start:all": "concurrently \"npm run backend\" \"npm run frontend\"",
    "backend": "cd back-end && npm install && npm run dev",
    "frontend": "cd front-end/vite-project && npm install && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
