{
  "name": "ai-career-guidance",
  "private": true,
  "scripts": {
    "start": "npm run start:all",
    "start:all": "concurrently \"npm run backend\" \"npm run frontend\"",
    "backend": "cd back-end && npm install && npm run start",
    "frontend": "cd front-end/vite-project && npm install && npx vite build && npx serve -s dist -l 3000"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
