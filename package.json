{
  "name": "ai-career-guidance",
  "private": true,
  "scripts": {
    "start:all": "concurrently \"npm run backend\" \"npm run frontend\"",
    "backend": "cd backend && npm install && npm run start",
    "frontend": "cd front-end && npm install && npx vite build && npx serve -s vite-project/dist -l $PORT"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
